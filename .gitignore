# --- Executables ---
*.exe
*.exe~
*.dll
*.so
*.dylib
*.apk
recipe-tracker

# --- Go specific ---
vendor/
# we keep go.sum for reproducible builds

# --- Elm specific ---
**/elm-stuff/
# We keep js/elm.js because Go embeds it, but exclude other temporary artifacts
index.html.js

# --- Project Specific Data ---
# We ignore the local database, logs, and user configuration
recipes.db
test_recipes.db
tracker.log
config.json

# Ignore generated output folders
Printables/
backups/

# --- OS / IDE specific ---
.DS_Store
.vscode/
*.swp
*.bak
*.log
